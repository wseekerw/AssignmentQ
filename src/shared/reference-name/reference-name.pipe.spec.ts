import { ReferenceNamePipe } from './reference-name.pipe';

describe('ReferenceNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ReferenceNamePipe();
    expect(pipe).toBeTruthy();
  });
});